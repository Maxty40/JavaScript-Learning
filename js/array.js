// Array can store more than 1 data/index
let path = ['Trailblaze', 'Hunt', 'Elation']

alert(path)

// Pick spesific index from array
// alert(path[1])

// Adding new index to array
path.push('Harmony', 'Preservation')

// Removing first index
path.shift()

// Removing last index
path.pop()

alert(path)

// Another way to create an array
let faction = []

// Inserting data to array index
faction[0] = "Belobog"
faction[1] = "Xianzhou"
faction[2] = "Penacony"

alert(faction)